Resolution:
1920 x 1080, duplicate display

Sound:
Test with the participant

Install ffmpeg before running

The python file is in the following folder:
aomic\movie_aomic

Compatibility: Expyriment 0.9.0 or newer / Python 2.7

To launch the protocol, run the script "movie_protocol.py" from the movie_aomic directory. 
$ python movie_protocol.py 

There is only one run that is 11 minutes long.

The log files are generated by either running the script till the end or pressing the "ESC" at any time.
